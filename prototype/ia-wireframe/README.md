# Paper30Min PROTOTYPE（一次性原型，issue #44 + 后续美化任务）

将 [#43 应用组织与导航结构定稿决议](https://github.com/attackingjensen/paper-30min/issues/43) 可视化为可点击线框。**不是正式实现，不合入主线。**

## 文件

- `index.html` — 阅读界面原型（#44，已评议关闭：**B 双栏代码库式**入选进 #54 规格；双侧栏已按后续评议升级为 VSCode 式：左节树栏与右 PDF 对照栏均可收起（收起态 = 视口对应边缘的圆形展开浮钮，左 » 右 « 镜像对称）、均可拖拽调宽；已叠加 soft-ui 美化皮肤）。
- `login.html` — 登录初始界面原型（美化任务）：底色与正式页面一致，右侧登录栏（服务器/账号/密码 + 「暂不登录，独立运行」），左侧 Canvas 伪三维动态图案，**已选定「粒子浪潮」**（默认即此；`?v=1` 粒子神经球 / `?v=2` 环面纽结流光 仍可按 ←/→ 回看，鼠标视差）。登录功能属云端线未来工作，本原型只定视觉方向。

## 运行

双击打开 `index.html`（单文件，无依赖、无构建）。

- 底部悬浮条 `‹ ›` 或键盘 `←`/`→` 切换三个变体，URL `?variant=A|B|C` 可分享定位。
- 三个变体共享同一套决议（四 tab = 地图/原文/提问/回想卡片、PDF 对照侧栏、节页构成、书库/任务中心呈现），**只在地图页与节导航的结构上不同**：
  - `A` 文档式：单栏阅读流，节树（阅读路径）在 L1 内容之后。
  - `B` 双栏代码库式：节树常驻左栏（aider repo map 式），右栏随选中切换地图/节页。
  - `C` 仪表盘式：进度环 + 卡片网格，节卡片直接呈现 gist 与状态。
- 可交互 demo：tab 切换、节树点击进节页、已读完标记开关（联动进度与状态点）、未深挖节的「开始深挖」、PDF 对照侧栏开关与**左缘拖拽调宽**（默认约窗口 46%、最小 390px——现行实现为 46% 比例宽 + 缩放按钮，拖拽为新增提案）、原文翻译对照开关、地图页「（demo：直接看已建图态）」可回看建图前落地页。
- 数据为写死的示例（Attention Is All You Need），非真实产物。

## 评议关注点

- L1 地图一屏信息密度是否合适；节树状态信号（灰/橙/绿点）是否足以承担"导航即协议"。
- 节页自上而下（L2 卡 → 深挖区 → 图表区 → 标记开关）的秩序。
- 出处指针三分定位（文本→原文 tab、图表→节页图区、页→PDF 对照列）是否直觉。
- PDF 对照栏宽度：默认约窗口 46%（现行比例）+ 左缘拖拽可调是否顺手；拖拽提案是否进规格（待评议确认，暂定入 #54 呈现契约）。

评议结论回帖 #44；微调落入规格化票 #54。

## soft-ui 皮肤（2026-09-10 叠加）

按作者指定的 [stylekit soft-ui](https://www.stylekit.top/zh/styles/soft-ui) 参考叠加于 `index.html`（CSS 覆盖层在样式表末尾，可整块移除对比）：浅底 #f8fafc、主色 indigo #6366f1、卡片 20px 大圆角 + 彩色柔影、按钮上浮/按压缩放反馈、输入框灰底聚焦光环；状态语义色对齐 soft-ui accents（已标记 #10b981 / 已深挖 #f59e0b）。**该视觉方向属打磨批"视觉系统重整"素材，随打磨批重估进入规格，不混入 #54 的结构契约。**
