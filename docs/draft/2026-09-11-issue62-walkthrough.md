# Issue #62 真实窗口走查：进度派生改标记源与打卡写入缝（JS）

> 日期：2026-09-11。环境：`npm run tauri dev`（debug 构建），数据根 `%APPDATA%/com.paper30min.reader`。
> 走查前书库为 v3 真实库（2 篇论文、7 条精读结果、无部分结果），应用启动触发 schema v4 迁移。

## 迁移后旧数据核对（v3 → v4）

- 启动后 `PRAGMA user_version` = 4；`read_marks` 回填 7 行，`marked_at` 逐行等于对应精读结果的 `updated_at`（SampleNet 6 节 + 纯文本论文摘要）。
- `activity_days` 回填：import 日取 `added_at`（2026-09-05 / 2026-09-06）、analysis 日与 mark 日取结果 `updated_at`（均 2026-09-06，同日同 kind 去重）。
- 库内无中断部分结果，负例（不迁移为标记）由 `schema_v4_contract.rs` 夹具覆盖。
- 迁移后 SampleNet 全部 6 节有标记 → 书库卡片六个进度点全绿并出现「已读完」徽章；纯文本论文摘要带标记、其余无。

## 进度与筛选呈现

- 顶栏徽章随标记派生：迁移后「连续 0 天 · 已读 2 篇 · 已读完 7/8 节」；阅读头元信息「已读完 2/3」同口径。
- 书库新增「全部进度」筛选：选「已读完」只剩 SampleNet；选「未读完」只剩纯文本论文；× 重置后两篇都回来。

## 标记 / 撤销

- 精读卡片头部新增过渡入口（最终位置归 #56 节页票）：未标记显示「标为已读完」，已标记显示「✓ 已读完」章 +「撤销已读完」。
- 对「第 2 部分」点「标为已读完」：卡片就地出现已读完章、阅读头变 3/3、徽章变 9/9 且连续 0→1 天；SQLite 核对：`read_marks` 新增 `method` 行（marked_at 为点击时刻），`activity_days` 新增当日（2026-09-11）`mark` 行。
- 再点「撤销已读完」：章消失、阅读头回 2/3、徽章回 8/9，连续天数保持 1 天；SQLite 核对：`method` 标记行删除，当日 `mark` 活动日保留（撤销不回收）。

## 走查中发现的既有缺陷（本票顺带修复）

- 纯文本论文卡片进度点 2/3、徽章 8/9，与数据库 1 行标记不符：手工构造的迁移夹具把 `abstract` 写进了 `parts`，`readingParts` 投影前置摘要后又原样带上，同一部分身份被计两次（旧的结果派生进度同样受影响）。已在 `readingParts` 按部分身份去重（`abstract` + `part-N` 只取首次出现），修复后徽章 7/8、卡片 2 点（1 绿 1 灰）。真实解析路径（PDF / arXiv / 纯文本）从不产出 `abstract` id 的 part，属防御性修复。
