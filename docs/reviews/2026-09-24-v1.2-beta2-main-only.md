# v1.2.0-beta.2 主程序单包发布前审查

审查范围：相对 `0d7e218` 的 Beta.2 版本、发布清单校验、测试与发布说明改动。审查在 Beta.2 安装包构建前完成。

## 结论

未发现发布阻断项。主程序升至 `1.2.0-beta.2`，组件常量、内置清单与下载地址继续指向 `1.2.0-beta.1`；已安装组件仍按 Beta.1 版本识别，新用户仍可从 Beta.1 发布页下载。发布清单继续校验旧组件 ZIP 与内置哈希，同时按 Beta.2 版本核对新安装包签名、文件名和更新 URL。

审查指出发布说明提前声称产物清单已验证，已在构建前修正。Beta.1 组件 ZIP 的本地 SHA-256 与内置清单一致，发布清单单测 15/15 通过。代码审查结束后才构建 Beta.2 主程序；安装包实际签名、文件名、版本及 `latest-beta.json` 均经发布工具复验。

[Beta.2 预发布](https://github.com/attackingjensen/paper-30min/releases/tag/v1.2.0-beta.2)仅上传主程序安装包、`.sig` 和 `latest-beta.json`。重新下载的安装包 SHA-256 为 `602DFD323C2DB1EF487DB2311114D383C3D1B7BBA54BC3136828CE688F0D4144`，与本地产物一致；远端签名及清单复验通过。

用户在云电脑安装版提供的任务中心截图中，两条翻译任务的模型首个响应分别为 0.5 s 和 0.7 s，总耗时分别为 7.3 s 和 27.6 s；对应输入/输出分别为 543/474 和 2278/2112 token。Beta.1 的数分钟首字等待在这次使用中未再出现。

## 待验证

- Beta.2 安装版的旧组件识别及书库保留仍待单项确认。GitHub CI 因账户问题不可用，不记为通过。
