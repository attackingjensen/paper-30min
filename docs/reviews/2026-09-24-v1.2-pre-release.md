# v1.2.0 发布前代码审查

2026-09-24 对 `steven123397/dev` 工作区相对 `origin/main` 的实现进行代码审查和独立复核。结论：**暂不能发布**。本记录保留交接要点；修复后须复审并重建安装包与签名。

## 发布阻断项

- [#93](https://github.com/attackingjensen/paper-30min/issues/93)：更新安装的活动检查与任务、组件操作的准入之间存在竞态，可能中断新启动的操作。
- [#94](https://github.com/attackingjensen/paper-30min/issues/94)：组件下载没有连接及停流超时，阻塞读取期间取消不能及时生效。
- [#95](https://github.com/attackingjensen/paper-30min/issues/95)：发布清单工具只检查 `.sig` 非空及文本一致，未验证签名与安装包字节匹配。
- [#96](https://github.com/attackingjensen/paper-30min/issues/96)：完整重建失败后的重试未传递覆盖确认，已有地图时会被 Rust 拒绝。

## 其他发现与验证缺口

- 组件自检子进程无超时与取消；关闭窗口保护未覆盖组件操作；旧组件状态可能覆盖实时安装进度；更新预检期间可重复点击安装。修复时补组件命令生命周期测试。
- 同论文并发建图的部分写入风险在 `origin/main` 已存在，未归因于本轮改动，仍需另行处理。
- 本次提交前 `node --test` 为 224/224、语法检查为 128 个文件、smoke 为 14/14、组件打包测试 6/6、发布清单测试 3/3 通过。`cargo test` 全量运行在 `resident_second_convert_has_zero_startup` 失败（第二次转换报告 `resident=false`）；单独复跑通过，需排查稳定性并重新完成全量运行。
- v1.1.0 默认/自选目录原地升级、签名安装版更新、干净安装、组件安装/卸载及旧书库一致性副本尚未完成走查。私钥独立备份尚未确认；GitHub CI 不可用。

修复上述阻断项后，先执行相关测试和复审，再生成用于正式发布的安装包、组件与更新清单，并完成安装版验收。
