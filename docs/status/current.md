# 项目当前状态

> 更新时间：2026-09-11（#68 提问 UI 行为契约完成）

## 当前阶段

项目已完成从浏览器阅读器到 Windows Tauri 客户端的首轮工程化闭环。Windows 端目前达到“能用”，尚未达到“好用”；当前工作重点是实际使用、收集可复现的体验问题，再决定打磨优先级。

打磨批整改草稿已形成：[2026-09-07 打磨批整改草稿](../draft/2026-09-07-polish-batch-draft.md)，覆盖 UI 层静默行为、阅读流、任务中心与导航（含 #33）、Paper30Min 改名与新图标、视觉系统重整；解析器止血、输入保真（#34）与分析架构重组列为方向区。新图标已选定 B「30 分钟进度环」。因分析架构可能带来功能级调整，作者决定暂缓收集多方意见，草稿不转为正式规格与票据；分析架构重新设计已立 Wayfinder 地图：[#35 Paper30Min 精读 harness 重新设计](https://github.com/attackingjensen/paper-30min/issues/35)，项目性质由此从"论文阅读工具"转变为"论文精读"专精 agent harness（帮助用户高效阅读一篇完整论文）。四张调研票（#36–#39）已回；核心设计票 [#41 论文作为代码库](https://github.com/attackingjensen/paper-30min/issues/41) 已定稿关闭：论文表示定为 MD 块模型 + 图表/参考文献清单 + 页图通道（块级出处、统一出处语法），PDF 生产走 Docling 侧车 + 自研参考文献规则层，模型侧四件抓取工具 + L1 常驻地图，深挖为"配方打底 + 工具越界"循环，L1/L2 自底向上生成、产物条条带出处，技能库改为建图/深挖/综合协议提示词 + 章节关注点。Docling 落地验证票 [#46](https://github.com/attackingjensen/paper-30min/issues/46) 已实测关闭（[验证报告](../background/2026-09-09-harness-validation/docling-sidecar-validation.md)）：10 篇踩坑论文集全过（无编号/罗马/双栏/附录全检出、prov 词覆盖 94–97%、CPU 3.0–10.1 s/页、随包增量约 +0.8–1.0GB），百度云端对照中 PP-StructureV3 因坐标帧系统性偏移仅作 BYOK 降级预案、PaddleOCR-VL 因静默截断+生成式不翻盘；规格化票 [#47](https://github.com/attackingjensen/paper-30min/issues/47) 已关闭，定稿规格发布为 [#48 Spec：PDF 块模型生产管线](https://github.com/attackingjensen/paper-30min/issues/48)（ready-for-agent，可拆实施票；遗留风险=Docling 模型权重再分发许可待核实）；进度数据模型票 [#40](https://github.com/attackingjensen/paper-30min/issues/40) 已定稿关闭——已读完=手动标记（可撤销、不以精读结果为前提、论文级派生态），新表 read_marks 随整记录写入缝落库，进度改由标记派生而阅读位置表不动，打卡落库 append-only 活动日表（撤销不回收），schema v4 按快照判据回填且失败拒启动，`CONTEXT.md` 术语已同步；其规格化票 [#49](https://github.com/attackingjensen/paper-30min/issues/49) 已关闭，定稿规格发布为 [#51 Spec：进度与已读完数据模型](https://github.com/attackingjensen/paper-30min/issues/51)（ready-for-agent）；提问契约票 [#42](https://github.com/attackingjensen/paper-30min/issues/42) 已定稿关闭：绑定双形态=@节（@补全 chip+节上入口）与原文选中浮动按钮（PDF 框选不做），单消息单绑定、问答 v1 不挂工具；配方=@节 L1+L2+节原文全送不带页图、片段块并集+覆盖图表自动附裁切图（问答唯一图像通道）、全文=L1+整篇原文；三形态单会话流、12 条历史且绑定注入不重放、统一建图门禁、超输入硬顶报错不截断；chat_messages 增绑定列，气泡 chip/折叠引用块可定位，`CONTEXT.md` 增「绑定提问/全文提问」；其规格化票 [#50](https://github.com/attackingjensen/paper-30min/issues/50) 已关闭，定稿规格发布为 [#52 Spec：提问双形态契约](https://github.com/attackingjensen/paper-30min/issues/52)（ready-for-agent）；规格化过程中发现 #41 模型侧（建图编排、四件抓取工具、L1/L2/L3 产物契约、技能库四段提示词）尚无规格化票，已补立 [#53](https://github.com/attackingjensen/paper-30min/issues/53) 进入前沿；信息架构票 [#43](https://github.com/attackingjensen/paper-30min/issues/43) 已定稿关闭：骨架=地图落地页+节页两层导航（四 tab=地图/原文/提问/回想卡片，PDF 对照侧栏保留，翻译降为原文内节级对照开关），产物居住=L1 地图页、复述稿地图页底部、L2 节页置顶、L3 节页深挖区、原文全局 tab 一处居住（保留跨节片段选择前提）、回想卡片留 tab 且生成源切协议产物，导航即协议=节树状态信号（未深挖/已深挖/已标记）+建图前落地页门禁呈现+出处定位三分（文本块→原文 tab 高亮、图表→节页图区、页→PDF 对照列），书库增建图状态呈现，任务中心单任务粒度+深挖步骤流+批量编排子进度，阅读位置视图值域扩展；UI 原型票 [#44](https://github.com/attackingjensen/paper-30min/issues/44) 已评议通过关闭：三变体可点线框（`prototype/ia-wireframe/`，A 文档式 / B 双栏代码库式 / C 仪表盘式）中选定 **B 双栏代码库式**（节树常驻左栏、状态信号常显）为地图页/节导航结构，并新增 PDF 对照栏左缘拖拽条款（保留 46% 比例宽 + 缩放按钮，拖拽默认约 46%、最小 390px、最宽 75%）；其规格化票 [#54](https://github.com/attackingjensen/paper-30min/issues/54) 随之解除阻塞；规格化票 [#53](https://github.com/attackingjensen/paper-30min/issues/53)、[#54](https://github.com/attackingjensen/paper-30min/issues/54) 均已关闭，定稿规格发布为 [#55 Spec：建图协议与 L1/L2/L3 产物契约](https://github.com/attackingjensen/paper-30min/issues/55)（三个领域任务种类、工具调用走文本协议、产物表 protocol_products、技能库四段提示词+关注点）与 [#56 Spec：应用组织与导航结构](https://github.com/attackingjensen/paper-30min/issues/56)（B 双栏四 tab、节页三态深挖区、侧栏浮钮、出处三分、书库建图状态、旧结果只读折叠区），均 ready-for-agent；汇总票 [#45](https://github.com/attackingjensen/paper-30min/issues/45) 已收官——**#35 地图收敛关闭**：16 张实施票 [#57–#72](https://github.com/attackingjensen/paper-30min/issues) 已拆出，均以子 issue 挂到所属规格并接好原生 blocked-by 依赖；ADR [0006](../adr/0006-three-layer-reading-protocol.md)（三层阅读协议）与 [0007](../adr/0007-progress-and-products-data-model.md)（进度与产物数据模型）已落；CONTEXT.md 复核无缺口。**实施链第一票 [#57](https://github.com/attackingjensen/paper-30min/issues/57) Docling 侧车打包与进程契约已完成**：嵌入式 Python 3.11.9 + 钉版依赖（与 #46 venv 逐版一致）+ 布局/表格/OCR 模型的随包案（落盘 1504MB、NSIS 安装包整体 728MB 实测打包成功，在 #46 估算带内）与首启下载案（36MB + bootstrap 在线补齐，HF_ENDPOINT 可配、hf-mirror 回退实测走通）双双落地；`pdfparse.convert@1`/`pdfparse.bootstrap@1` 任务接入任务中心生命周期（取消=杀子进程），`pdfparse.status@1` 与 `settings.putPdfparse@1`（hfEndpoint）上线；三组模型权重许可逐款核实均可再分发（[核实留档](../background/2026-09-10-docling-sidecar/model-license-review.md)，#48 遗留风险关闭）；OCR 按规格门禁为"仅无文本层页触发"（顺带修掉了 Docling 默认对插图位图逐页 OCR 的数倍减速）；热态实测 2.5–7.8 s/页、全部在 #46 基线 1.5 倍内（[体积与速度复核留档](../background/2026-09-10-docling-sidecar/packaging-measurements.md)）；契约测试 `pdfparse_contract.rs` 8 项全过（夹具 PDF→子进程→结构化输出/错误码/取消）。**[#58](https://github.com/attackingjensen/paper-30min/issues/58) DoclingDocument → 块模型映射层已完成**：`src/pdfmap.rs` 纯函数（输入侧车 docling.json，输出 MD 块模型 + 图/表/参考文献三清单），覆盖 #48 决策 6–12——标签映射（小节标题/自然段/图占位/表 MD/公式占位/脚注、furniture 剔除保留、CAPTION 并入）、prov 换算（BOTTOMLEFT pt → pdf.js 视口 scale=2，跨页记首页）、阅读顺序 body 树 DFS（picture/table children 不展开，图内文字碎片与表格单元格碎片按 bbox 区域剔除）、节层级状态机（阿拉伯/罗马体系 + 白名单 + 字高回退，Docling level 字段恒 1 不可用）、附录区图题误判过滤（流内夹图降级）、图表清单 ID 从图注编号派生（漏检不漂移、无编号不进清单、frontmatter 区图表同样收录）、参考文献规则层（[n]/n. 编号制判定、按序补号、作者-年份制降级空清单、正文 [n] 引用点扫描）；fixture 测试 5 篇（#46 实测 JSON gzip 固化）+ 内联单测 23 项全过。**[#59](https://github.com/attackingjensen/paper-30min/issues/59) 页图与图表裁切预渲染已完成**：侧车新增 `render` 子命令（pypdfium2 + Pillow，不依赖布局/OCR 模型），按映射层换算后 bbox 渲染页图（scale=2 webp q86，1224×1584）与图/表/公式裁切图；新任务 `pdfassets.prerender@1`（输入 paperId + doclingJsonPath + 可选 pdfPath）驱动"映射 → 渲染 → 逐件走附件缝落库（sha256 + 原子写 + 事务）+ verify 读回校验"全链；附件 ID 约定 `pageimg-{NNNN}`（页码零填充）/ `crop-{fig|tbl …}` / 公式 `crop-fml-{节id}-b{块号}`（#48 决策 13 公式裁切在本票补齐），块模型 JSON 随本任务落 `blockmodel.json` 附件（#48 决策 5"解析产物入书库"在此闭环，#55 preflight 与四件工具的数据源）；公式裁切坐标与页图坐标系经真实论文（1706.03762 Figure 1）视觉验证贴合；arXiv HTML 图直链按"用到才下载"由既有 files.download@1 承载，不进预渲染管线。契约测试 `pdfassets_contract.rs` 5 项全过（含 verify 缝逐件校验、页图 1902 token/页预算与 #38 实测一致、幂等重跑）。**[#60](https://github.com/attackingjensen/paper-30min/issues/60) 踩坑论文集回归夹具与性能门禁已完成**：#46 的 10 篇踩坑论文 PDF 原样入库（`app/src-tauri/tests/fixtures/regression/`，含 manifest 基线清单与 README），`pdfparse_regression.rs` 提供三种运行模式——始终运行的清单良构校验（版本钉三方一致、夹具在场，node 侧另有 CI 可跑的同构校验）、`PAPER30MIN_PDFPARSE_FIXTURES=1` 全链 assert（逐篇 PDF→侧车→映射→基线断言+性能门禁，本机实测 13/13 全绿、性能 0.27–1.03× 基线）、`=dump` 基线重建（支持复用既有 docling.json 目录）；引擎不变量覆盖 prov/占位/MD 表/无空块/furniture 泄漏探针/参考文献补号连续/编号节阅读顺序；Docling 版本锁定经 manifest↔requirements 交叉校验，升级流程已写入 `tools/pdfparse-sidecar/README.md`。公式密集样例三类齐备：文字公式=1412.6980、双栏布局=1603.02754、矢量与图片公式=合成夹具（生成器+固化 fixture+「不产失真文本、检出即占位+bbox」断言），扫描件降级提示由既有 OCR 契约测试承载。基线构建过程中发现并修复映射层两处缺陷：编号附录（"10 APPENDIX"）被降为 References 小节 → 开附录节（Adam 附录存活，US3）；编号文献节（"8. REFERENCES"）角色未归位 → References 角色（XGBoost 26 条参考文献建表，US8）。**[#61](https://github.com/attackingjensen/paper-30min/issues/61) read_marks 与 activity_days 建表、schema v4 迁移与整记录写入缝已完成**：新表 `read_marks(paper_id, part_id, marked_at)` 与 append-only 的 `activity_days(day, paper_id, kind)`（kind ∈ import/analysis/partial/mark）随 `putPaper` 整记录写入缝落库——标记按 DTO 快照整组重写（撤销=快照少一行）、活动日 INSERT OR IGNORE 幂等并入且 DTO 未携带的历史行不动；schema v4 迁移单事务完成，按快照判据回填（正文不以警示后缀结尾的 analyses 行 → 标记且 marked_at 取该结果 updated_at，中断部分结果不迁移；addedAt → import 日、各结果 updatedAt → analysis/partial 日、迁移新建标记 → mark 日，日界取 UTC 日历日），判据常量写死在迁移代码、失败整体回滚并拒绝启动；DTO 与整库导出携带两表、导入按主键幂等合并（标记冲突保留较早 marked_at）；前端 store 双向映射贯通新字段、`applyResplit` 让标记随结果一并作废（四写入缝的打卡生成与进度派生切换归 #62）。契约测试 `schema_v4_contract.rs` 5 项（v3 快照三类夹具、同日活动日去重、重开不重复迁移、失败回滚拒启动）+ library/migration 契约各新增 5/2 项全过。**[#62](https://github.com/attackingjensen/paper-30min/issues/62) 进度派生改标记源与打卡写入缝（JS）已完成**：阅读进度与书库"已读完"筛选数据源从 analyses 换为 read_marks（`readingProgress`/`isPaperRead` 前端派生不落库；`readingParts` 顺带按部分身份去重——手工构造的迁移夹具把 abstract 写进 parts 导致投影重复计一次，旧派生同样受影响）；四个写入缝（导入 createPdfPaper/createArxivPaper、saveAnalysis 完整/带 `{partial:true}` 中断保留、setReadMark 设置标记）各自把当日活动日行并入记录 DTO，同日同 kind 幂等、日界取事件时间戳的 UTC 日历日（与 v4 迁移回填同一口径），撤销标记不写不回收；打卡徽章与阅读头改从落库活动日派生，重读旧节不再改写历史打卡；分节卡片头部新增"标为已读完/撤销已读完"过渡入口与已读完章（最终位置归 #56 节页票），书库新增"已读完/未读完"筛选与已读完徽章。JS 契约测试 `papers.test.mjs` 新增 11 项（派生 3、四写入缝 5、打卡派生 1、投影去重 1、标记落库失败回滚 1）全过；真实窗口走查含 v3→v4 真实库迁移回填核对与标记/撤销 SQLite 逐行对账（[走查记录](../draft/2026-09-11-issue62-walkthrough.md)）。**[#63](https://github.com/attackingjensen/paper-30min/issues/63) protocol_products 产物表与 DTO 写入缝已完成**：新表 `protocol_products(paper_id, kind, part_id, body, updated_at)`（kind ∈ map/l2/dig/retell，主键 `(paper_id, kind, part_id)`，随论文级联删除；part_id 空值约定——论文级产物 map/retell 为空串、节级 l2/dig 必为精读部分 id）随 schema v5 迁移建表（无历史回填，单事务失败整体回滚拒启动）；PaperDto 增 `products[]` 随 `putPaper` 整记录写入缝按快照整组重写（重跑覆盖、撤销/重做不留版本），body 为 JSON 值（map/l2 结构对象、dig/retell Markdown 字符串），库内 TEXT 存序列化 JSON；normalize 校验 kind 四值、partId 空值约定、body 非空值、updatedAt ISO 与 `(kind, partId)` 唯一；整库导入清洗畸形条目并按主键去重保留较新 updatedAt，JS store 双向映射进出共用一份形状过滤（仅 updatedAt 毫秒↔ISO 分方向）；`applyResplit` 让消失部分的 l2/dig 产物随结果一并作废（规格 #55 决策 22，map/retell 论文级保留），整库导出信封自动携带。契约测试 `schema_v5_contract.rs` 4 项（v4 快照迁移后产物往返、重开不重复迁移、冲突表失败回滚拒启动、空库迁移）+ library 契约新增 4 项（四 kind 往返、快照重写覆盖、级联删除、六类非法拒收）+ migration 契约新增 2 项（携带去重、畸形清洗）全过。**[#64](https://github.com/attackingjensen/paper-30min/issues/64) 技能库改造：四段协议提示词与章节关注点已完成**：技能库从"每类章节一个提示词"改为四段协议提示词 `map-l2`/`map-l1`/`deep-dive`/`synthesize`（仓库根 `skills/*.md`，各内嵌任务/工具指导/出处纪律/输出格式必备段与随新上下文形状定义的占位符集合，token 预算写在文件头部注释）+ 章节关注点数据化（`skills/section-focus.json`，五类受控类型关注点清单、内容自旧五技能迁来，类型词表即建图调用①打标取值域）；运行时叠加规则 = 协议提示词打底 + 该节类型关注点追加（map-l2 叠加全表、deep-dive 叠加本节类型、map-l1/synthesize 不叠加；覆盖版提示词丢 `{sectionFocus}` 占位符时关注点块仍追加到末尾）；旧五技能文件迁至 `skills/legacy/` 留档并继续驱动浏览器阅读器（server.py 改指 legacy/）与旧精读生成路径（后者随 #55 决策 26 退役前用户调教不失效）；覆盖对象升级 v2（`{version, prompts, focus, legacy}`，`skills.list@1` 与 settings overrides 缝沿用），v1 平铺覆盖在 initSkills 时迁移写回——已知类型迁为关注点覆盖项 + legacy 只读留档、未知键仅留档，legacy 同时供旧精读路径沿用；skills.list@1 现返回 10 个文件，前端按文件名路由（`legacy/` 前缀 → 旧技能、`.json` → 关注点数据、其余 .md → 协议提示词）。JS 测试 `skills-sync.test.mjs` 重写为 26 项（结构断言：必备段落/必备占位符/统一出处语法/四件工具段；防漂移：三组内置兜底 ↔ 文件；叠加规则 4 项；迁移夹具与导入 6 项；加载路由与兜底 3 项）全过，Rust `skills_contract.rs` 与 smoke 同步更新。**[#65](https://github.com/attackingjensen/paper-30min/issues/65) 领域任务种类与文本协议工具循环已完成**：新增 `paper.build-map@1` / `paper.deep-dive@1` / `paper.synthesize@1` 三个领域任务种类（`src/protocol.rs` + tasks.rs 注册），深挖单节与批量同构 partIds[] 逐节推进；文本协议工具循环（```tool 围栏块解析、连续 2 次解析失败收尾、步数上限 12）由 Rust 驱动，四件工具纯数据读（read_section 8KB 块窗+spill 页脚 / search_paper 指针+超帽轮转 / get_figure 与 get_page_image 随观察附图），工具轨迹与阶段事件经 TaskEvent.detail 进事件流、中间轮不产对外 chunk；建图编排 preflight 齐备校验（preflight_missing 即指令）→ 调用① L2 全覆盖（超 ~20 万 token 按节分片、确定性合并）→ 调用② L1 合成，任一调用装配估算超 983,616 即 input_too_large 拒绝（不截断）；取消 = 当前步完成后停止（轮边界检查点）：深挖逐节完成即落库（analysis 打卡），建图在调用②前取消时已产出 L2 以 partial:true 落库并计 partial 打卡；重跑覆盖确认 already_exists、未建图门禁 map_required/l2_missing；精读部分 ↔ 块模型节按顺序对应（abstract ↔ Abstract 节、part-N ↔ 第 N 个内容节），不一致记 warning；决策 16 的要点 3–6 条/gist ≤2 句按软约束 warning 落实。初始配方组装为 JS 纯函数缝 `app/ui/js/protocol.js`（配方拼装/分片合并/出处解析校验/调用块解析/产物 schema 校验），与 Rust 侧共享常量与同式规则；块模型磁盘夹具首例落 `src-tauri/tests/fixtures/protocol/blockmodel-basic.json`；契约测试 `protocol_contract.rs` 14 项（mock 模型端点脚本化驱动工具循环/取消/上限/门禁/分片/硬顶）+ JS `protocol.test.mjs` 14 项全过。**[#66](https://github.com/attackingjensen/paper-30min/issues/66) chat_messages 绑定列与迁移已完成**：schema v6 给 `chat_messages` 增列 `binding_kind`/`sec_id`/`fragment_text`/`cite_json`/`asset_ids_json`，ALTER 带 DEFAULT 使旧行回填 `none`；`ChatMessageDto` 同步携带绑定字段（`cite` 为块区间起止节/块/页，`assetIds` 为附图列表），assistant 消息恒 `none`；随 `putPaper` 整记录写入缝往返，淘汰最旧消息时绑定一并删除（仍由 JS 40 条上限 `slice(-40)` 裁剪快照）；整库导入缺省回填 `none`、畸形绑定清洗为 `none` 而不拒收整篇。契约测试 `schema_v6_contract.rs` 4 项 + library 新增 4 项 + migration 新增 2 项 + JS store 绑定往返 1 项全过。**[#67](https://github.com/attackingjensen/paper-30min/issues/67) 三形态上下文组装纯函数已完成**：`app/ui/js/qa.js` 的 `assembleQaContext` 按 #52 决策 4–8 装配 messages——@节 = L1+该节 L2+节原文全送、片段 = L1+覆盖块并集+图表占位裁切图（问答唯一图像通道）、全文 = L1+整篇原文；历史取最近 12 条并重放为 `[@节名]` / `[引用："…"]` 标注文本、旧轮绑定注入不重放；未建图 `map_required`、装配超 983,616 `input_too_large` 硬顶拒绝不截断。现行 5K/节、24K 截断注入已从 Tauri `sendChat` 退役；`model.chat@1` 接受 OpenAI 多模态 parts 以便片段附图直送。JS `qa.test.mjs` 12 项全过，`model_contract` 新增多模态转发 1 项。**[#68](https://github.com/attackingjensen/paper-30min/issues/68) 提问 UI 行为契约已完成**：按 #52 决策 9–13，输入框 `@` 补全覆盖块模型全地址空间（含 References/Acknowledgments），选中成可删除 chip；精读卡片、原文节 chip 与节标题提供「提问」入口；原文块模型视图选中文本出现浮动「提问」，选区扩成完整文本块后跳提问 tab 带入折叠引用块；用户气泡 chip / 引用块点击定位原文并高亮对应节或块；未建图时问答入口禁用并提示先建图。发送/中断沿用 Enter 发送、离开阅读器中断。纯函数缝仍在 `qa.js`（补全/扩块/气泡呈现/门禁），DOM 编排在 `main.js`。JS `qa-ui.test.mjs` 9 项全过。走查清单见 [Issue #68 真实窗口走查](../draft/2026-09-11-issue68-walkthrough.md)（窗口点验待做）。实施前沿推进为 #69（视图状态机）。打磨批重估随地图收敛解禁，时点另定。

当前不扩展产品范围，不因零散感受新建打磨类 Issue，也不创建合入 `main` 的 PR。`steven123397/dev` 是当前协作分支。

## 已具备能力

### 浏览器阅读器

- 支持从 arXiv HTML、本地 PDF、示例论文和粘贴文本导入论文。
- 按论文实际一级章节组织精读部分，支持单节和批量精读。
- 支持 PDF 对照阅读、翻译、论文问答、论文整理和回想卡片。
- 支持 Markdown、LaTeX 公式、阅读进度、连续阅读统计和单篇笔记导出。
- 支持整库 JSON 导入/导出；导入按论文 ID 合并，API Key 不进入导出文件。

### Windows 正式客户端

- 采用 Tauri + Rust + 原生 JavaScript，SQLite 保存本地书库。
- 支持本地 PDF、arXiv 和示例论文导入，浏览器整库可预检后迁移。
- 支持论文、精读、PDF、翻译、问答和回想卡片的完整阅读流程。提问支持 @节 chip、原文选中片段引用块与建图门禁。
- 模型调用由 Rust 发起，API Key 只保存在本机设置表；流式输出通过任务事件传给界面。
- 任务中心支持排队、进行中、成功、失败、已取消、待重试等状态，以及取消、重试和关闭窗口时的等待/停止选择。
- 论文成果、附件、设置和阅读位置在关闭并重新打开后可恢复。
- 已完成第一轮渐进式前端重设计：书库、阅读和任务中心三视图，阅读位置可恢复，业务行为和数据契约与浏览器版保持一致。

## 工程验证

- 根目录 `npm test`：197 项通过（含 #68 提问 UI 纯函数 9 项；含 #67 三形态装配 13 项；含 #66 store 绑定往返 1 项；含 #65 协议纯函数缝 14 项；含 #60 回归夹具清单良构 3 项、#61 前端 applyResplit/导出携带 3 项、#62 进度派生与四写入缝活动日 11 项、#63 applyResplit 产物作废 2 项（导出携带断言并入既有用例）、#64 技能库结构断言/叠加规则/迁移夹具 26 项；根测试递归含 `app/tests/`）。
- `cd app && node --test`：114 项通过。
- `cd app/src-tauri && cargo test`：218 项通过（含 `model_contract` 19 项，#67 新增多模态转发 1 项；含 `schema_v6_contract` 4 项；含 `protocol_contract` 14 项与 protocol 内联 12 项；含 `pdfparse_contract` 8 项、`pdfmap` 映射层内联 25 项与实测 fixture 6 篇、`pdfassets_contract` 5 项、`pdfparse_regression` 13 项——默认跑清单良构，全链 assert 模式本机实测 13/13 全绿；`schema_v4_contract` 5 项、`schema_v5_contract` 4 项、`library_contract` 24 项、`migration_contract` 14 项、`skills_contract` 3 项随 #64 更新）。
- `cd app && npm run smoke`：14/14 通过。
- 真实 Tauri 窗口走查：设置、导入、精读、批量取消、翻译、问答、回想卡片、PDF 操作、任务中心、阅读位置恢复、关闭流程和重启恢复共 12 项通过。
- 详细走查记录见 [Issue #31 真实窗口走查清单](../draft/2026-09-06-issue31-walkthrough.md)。

## 实施状态

Windows 正式客户端规格 [Issue #24](https://github.com/attackingjensen/paper-30min/issues/24) 已随项目定性转变关闭（实施链主体已完成），实施链如下：

| Issue | 内容 | 状态 |
| --- | --- | --- |
| [#27](https://github.com/attackingjensen/paper-30min/issues/27) | Tauri 外壳与 Rust/JavaScript 桥接 | 已完成 |
| [#28](https://github.com/attackingjensen/paper-30min/issues/28) | 本地数据目录与 SQLite 书库 | 已完成 |
| [#29](https://github.com/attackingjensen/paper-30min/issues/29) | 附件与 PDF 文件能力 | 已完成 |
| [#30](https://github.com/attackingjensen/paper-30min/issues/30) | 浏览器书库迁移 | 已完成 |
| [#31](https://github.com/attackingjensen/paper-30min/issues/31) | 论文阅读、模型调用与任务生命周期 | 已完成 |
| [#32](https://github.com/attackingjensen/paper-30min/issues/32) | 安装、升级与正式本地验收 | 已关闭（未启动，随定性转变废止） |

云端同步服务和 Android 阅读伴侣目前只有正式规格，分别见 [Issue #25](https://github.com/attackingjensen/paper-30min/issues/25) 和 [Issue #26](https://github.com/attackingjensen/paper-30min/issues/26)，暂不进入实现。

## 已知未收口项

这些事项是当前事实或技术限制，不自动转化为新的需求：

- 任务注册表仍为内存态，应用重启后不恢复历史任务。
- 阅读位置尚未携带 `contentVersion`，也未接入云端同步。
- 本机 PDF 打开时暂时一次性读取文件，尚未实现按页懒加载传输。
- 移除回想卡片图片后，附件文件暂时没有清理命令。
- 精读节卡片联动 PDF 页码的代码已接入，但还缺一次人工点击确认。
- 安装包、覆盖升级、干净账户安装和卸载验收尚未完成，属于 #32 范围。
- #33（任务中心导航误取消任务）与 #34（公式密集论文 PDF 保真）已随 #35 地图收敛移交关闭：#33 语义经 #56 规格修订评论落 #69/#72 验收；#34 由 #48 管线的公式占位+裁切图策略覆盖，公式密集回归样例移交 #60，「编辑原文」入口暂不携带、实际使用仍痛再立票。

## 相关决策与维护规则

- 三端路线和客户端边界见 [正式客户端与前端边界规格](../background/client-and-frontend-boundaries.md)（已移入 background，作现状参考）。
- Rust/JavaScript 接口边界见 [本地能力接口规格](../background/client-local-rust-js-boundary.md)（同上）。
- 项目术语和领域约束见 [CONTEXT.md](../../CONTEXT.md)。
- 具体需求、决策和验收以 GitHub Issue、规格文件和走查记录为准；本文件只记录当前快照。
- 完成一项工作后，只在该工作改变当前目标、能力、风险、未完成事项或下一步时更新本文件；不要把提交记录、命令输出和过程日志复制到这里。
